<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/home_page_background_colour">

    <TextView
        android:id="@+id/filterHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="24dp"
        android:text="@string/filter_screen_title"
        android:textSize="22sp"
        android:gravity="center"
        android:textStyle="bold"
        android:fontFamily="@font/lexend_font"
        android:textColor="@color/text_black_colour" />

    <LinearLayout
        android:id="@+id/buttonContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="vertical"
        android:padding="24dp">

        <Button
            android:id="@+id/applyFilterButton"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:backgroundTint="#F05A09"
            android:text="Apply"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            app:cornerRadius="12dp" />

        <TextView
            android:id="@+id/cancelFilterText"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:gravity="center"
            android:padding="8dp"
            android:text="Cancel"
            android:textColor="@color/text_black_colour" />
    </LinearLayout>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/filterHeader"
        android:layout_above="@id/buttonContainer"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="24dp">

            <TextView
                style="@style/FilterLabelStyle"
                android:text="Type"
                android:layout_marginTop="0dp" /> <com.google.android.material.chip.ChipGroup
            android:id="@+id/filterTypeGroup"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:chipSpacingHorizontal="8dp" />

            <TextView
                style="@style/FilterLabelStyle"
                android:text="Basic" />

            <com.google.android.material.chip.ChipGroup
                android:id="@+id/filterBasicGroup"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:chipSpacingHorizontal="8dp" />

            <TextView
                style="@style/FilterLabelStyle"
                android:text="Advanced" />

            <com.google.android.material.chip.ChipGroup
                android:id="@+id/filterAdvancedGroup"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:chipSpacingHorizontal="8dp" />

            <View
                android:layout_width="match_parent"
                android:layout_height="32dp" />

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</RelativeLayout>